{
  "name": "odo-tasks",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check": "npm run build && cargo check --manifest-path src-tauri/Cargo.toml",
    "test:e2e": "playwright test",
    "test:mcp": "cargo build --manifest-path src-tauri/Cargo.toml && node scripts/test-mcp-conformance.mjs",
    "openspec": "openspec",
    "spec:list": "openspec list",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.6.0",
    "@playwright/test": "^1.61.1",
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
